name: Deploy Supabase Function

on:
  push:
    branches: [ main ]
    paths:
      - "supabase/functions/**"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      - name: ⚙️ Install Supabase CLI (stable + export PATH)
        run: |
          curl -sL https://github.com/supabase/cli/releases/download/v1.152.1/supabase_linux_amd64 -o ~/.local/bin/supabase
          chmod +x ~/.local/bin/supabase
        env:
          PATH: ~/.local/bin:$PATH

      - name: 🚀 Deploy summarizeMeeting function
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          PATH: ~/.local/bin:$PATH
        run: |
          supabase functions deploy summarizeMeeting --project-ref rpcypbgyhlidifpqckgl


